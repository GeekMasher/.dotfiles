#!/bin/bash 

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

if ! command -v gh codeql &> /dev/null; then 
    $SCRIPT_DIR/codeql-update
fi 

# pass all arguments to gh codeql
gh codeql-scan --disable-upload "$@"

